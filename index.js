
const { slugifyString, capitalizeWords } = require('strings.js');

const { orderByAsc, randomElem } = require('arrays.js');

module.exports = {
  orderByAsc,
  orderByAsc,
  slugifyString,
  capitalizeWords,
}
